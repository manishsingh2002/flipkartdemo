import { BodercolorDirective } from './bodercolor.directive';

describe('BodercolorDirective', () => {
  it('should create an instance', () => {
    const directive = new BodercolorDirective();
    expect(directive).toBeTruthy();
  });
});
